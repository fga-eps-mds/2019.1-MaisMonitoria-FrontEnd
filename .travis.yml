sudo: required

language: node_js

node_js:
  - "10.15.3"

services:
  - docker

branches:
  - configurar-ambiente

notifications:
  slack: hora-da-hora:0nFQxr2PykmKhNkJ78xYJMOP

before_script:
  - docker build -t frontend .

script:
  - docker run frontend yarn build
  
deploy:
  - provider: script
    script: bash scripts/deploy_image.sh
    on:
      branch: configurar-ambiente

addons:
  apt:
    packages:
      - docker-ce

